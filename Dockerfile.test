FROM node:22-slim
WORKDIR /app
ENV NEXT_TELEMETRY_DISABLED=1
COPY package.json package-lock.json ./
RUN npm ci --no-audit --no-fund
ENV PORT=3000
EXPOSE 3000
CMD ["node","-e","require(\"http\").createServer((q,s)=>s.end(\"ok\")).listen(3000,\"0.0.0.0\")"]
